#include "main.h"

/**
* main - tests function that prints if integer is positive or negative
* Return: 0
*/

void positive_or_nagative(int i)
{
if (i == 0)
{
printf("%d is zero\n", i);
}

return (0);
}
