#include "main.h"

/**
 * print_alphabet: It prints the alphabet in lowercase fallowed by a new line
 * Return: nothing
 */
void print_alphabet(void)
{
char l;

for (l = 'a'; l <= 'z'; l++)
{
_putchar(l);
}
_putchar('\n');
}
