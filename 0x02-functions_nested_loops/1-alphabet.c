#include "main.h"

/**
 * Description: It prints the alphabet in lowercase fallowed by a new line
 * Return: 0(success).
 */
int main(void)
{
char l;

for (l = 'a'; l <= 'z'; l++)
{
_putchar(l);
}
_putchar('\n');
return (0);
}
