#include "main.h"

/**
 * _abs - Returns absolute value of function argument
 *
 * @num: Convert this to absolute value
 *
 * Return: (0): Success
 */
int _abs(int num)
{
return (num < 0 ? (num * -1) : (num));
}
